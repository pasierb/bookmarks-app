<!DOCTYPE html>
<html>
  <head>
    <title><%=t :application_name %></title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">

    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-inverse">
      <div class="container">
        <div class="navbar-header">
          <%= link_to t(:application_name), root_path, class: 'navbar-brand' %>
        </div>

        <%= form_tag bookmarks_path, class: "navbar-form navbar-left", role: "search", method: 'GET' do %>
          <div class="form-group">
            <%= text_field_tag :search, params[:search], class: 'form-control', placeholder: t(:search, scope: 'text') %>
          </div>
          <button type="submit" class="btn btn-default"><%=t :search, scope: 'text' %></button>
        <% end %>

        <div class="navbar-right">
          <%= link_to t(:new, scope: 'pages.bookmarks'), new_bookmark_path, class: 'btn btn-primary navbar-btn' %>
        </div>
      </div>
    </nav>

    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
